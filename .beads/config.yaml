# Beads Configuration File
# See `bd config show` for full effective config.

issue-prefix: "lm"

# Dolt is authoritative; checkout hooks must not import legacy JSONL.
import.auto: false

# Wrix owns the shared Dolt server. Never start a competing host-side server.
dolt.mode: server
dolt.auto-start: false

# Git branch for beads data (used by bd dolt pull/push)
sync-branch: "beads"

# Sync configuration (dolt-native: bd dolt pull/push to the dolt-remote
# stored on the beads git branch worktree at
# .git/beads-worktrees/beads/.beads/dolt-remote/)
sync:
  mode: dolt-native

sync.remote: ".git/beads-worktrees/beads/.beads/dolt-remote"

# Do not regenerate or stage legacy JSONL during normal Dolt workflows.
export.auto: false
export.git-add: false
